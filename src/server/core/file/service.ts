//

export default class {
	//

	static storeFile(file: File, folderName: string): string {
		const path: string = null;
		//verify folder exists,
		//if not, then create
		//store image in folder
		//return image path
		return path;
	}
}
