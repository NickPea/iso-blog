//

export default class {
	//

	static sendEmail(
		recipients: string[],
		subject: string,
		template: string,
		parameters: any[]
	): boolean {
		const isEmailSent: boolean = false;

		//send email

		return isEmailSent;
	}
}
